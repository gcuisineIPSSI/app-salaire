node{
    stage('clone') {
        checkout scm
    }
    stage('Apache') {
      ansiblePlaybook (
          colorized: true,
          playbook: 'playbook.yml',
          inventory: 'hosts.yml'
      )
    }
}
